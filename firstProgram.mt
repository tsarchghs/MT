var a = 1;
var b = a;
var str = "string";
var str_copy = str;
var d = 1;
var c = 2 + 5 / 2;

// Example of nested if's
if d >= c: // If statements ends with the keyword 'end'
	if c <=d: 
		if c < d > c:
			d = 1;
		end
	end
end